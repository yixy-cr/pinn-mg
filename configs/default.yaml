model:
  hidden_dim: 128
  num_layers: 4
  activation: tanh

training:
  epochs: 1000
  learning_rate: 1.0e-4
  lr_decay: 0.98
  decay_interval: 2000

physics:
  lambda: 1.0
  mu: 0.35
  eq_weight: 1.0  # 启用物理约束

mesh:
  nx: 16
  ny: 16
  nz: 32

boundary:
  svr_C: 1.0
  svr_epsilon: 0.01